# Fonts

- `NotoSansTC-Regular.ttf` — Noto Sans TC (Traditional Chinese), SIL OFL 1.1 (`OFL.txt`).
- Subset built from the upstream variable Noto Sans TC so UI CJK glyphs and fullwidth punctuation (`：，` etc.) are present without shipping the full ~12MB face.
