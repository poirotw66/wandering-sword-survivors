# Fonts

- `NotoSansTC-Regular.ttf` — Noto Sans TC (Traditional Chinese), SIL OFL 1.1 (`OFL.txt`).
- Kept as a **glyph subset** (~200KB) covering strings in `godot/locales/ui_zh_TW.json` plus ASCII digits/punctuation.
- If new UI characters render as tofu, rebuild the subset with those glyphs and re-export.
